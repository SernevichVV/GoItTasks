/**
 * Created by Viktoria on 13.07.2016.
 */



/*
// check for valid input if it's empty string pf not a number
while (strNumb == "" || isNaN(strNumb) || strDegree == "" || isNaN(strDegree)) {
    // if it's ask user to enter value again do it until the valid value will be entered
    strNumb = (prompt("Please, enter the number", ""));
    strDegree = (prompt("Please, enter the degree", ""));
}
var numNumb = +strNumb;          // convert string to number,
var numDegree = +strDegree;
if (numDegree == 0) {            // if degree = 0, return 1 because every number in degree 0 equals 1
    console.log("Result is 1");  // we know it without calculations
}
else {
    var result = 1;
    while (numDegree > 0) {
        result *= numNumb;
        numDegree--;
    }
}

console.log("Result is", result);*/